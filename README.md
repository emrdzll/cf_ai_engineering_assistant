# cf_ai_engineering_assistant

An AI Engineering Assistant built on **Cloudflare Workers** with **KV Storage** and a **Space-themed UI**.

## 🚀 Features
- **Long-term Memory (KV):** Remembers user details.
- **Task Management:** Todo lists and scheduling.
- **Space UI:** Animated interface with Framer Motion.

## 🛠️ How to Run
1. **Install:** `npm install`
2. **Setup:** Create `.dev.vars` with `OPENAI_API_KEY=...`
3. **Run Locally:** `npm start`
4. **Deploy:** `npm run deploy`